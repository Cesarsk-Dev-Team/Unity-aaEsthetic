using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BackgroundSpawner : MonoBehaviour {

    private bool changeTrigger = true;
    //public Animator currentBackgroundAnimator;

    // Update is called once per frame
    void Update () {
		if(changeTrigger)
        {
            if(changeTrigger)
            {
                //currentBackgroundAnimator.SetTrigger("Fade Out");
                //changeTrigger = false;
            }
        }
    }
}
